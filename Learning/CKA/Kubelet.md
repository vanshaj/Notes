1. Loads or unloads containers on the nodes as informed from the scheduler via API Server
2. Kubelet registers node on the kubernetes cluster
3. Kubelet is not installed with kubeadm, we always have to manually install kubelet on the worker nodes
4. 